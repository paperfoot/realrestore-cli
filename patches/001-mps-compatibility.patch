diff --git a/diffusers/src/diffusers/models/realrestorer/layers.py b/diffusers/src/diffusers/models/realrestorer/layers.py
index b9dd921..1b2be6c 100644
--- a/diffusers/src/diffusers/models/realrestorer/layers.py
+++ b/diffusers/src/diffusers/models/realrestorer/layers.py
@@ -268,12 +268,17 @@ class MLPEmbedder(nn.Module):
 def rope(pos, dim: int, theta: int):
     if dim % 2 != 0:
         raise ValueError("RoPE dimension must be even.")
+    # MPS doesn't support float64 — compute RoPE on CPU with float64
+    # precision (proven FLUX pattern), then move result back to device.
+    original_device = pos.device
+    if pos.device.type == "mps":
+        pos = pos.to("cpu")
     scale = torch.arange(0, dim, 2, dtype=torch.float64, device=pos.device) / dim
     omega = 1.0 / (theta**scale)
     out = torch.einsum("...n,d->...nd", pos, omega)
     out = torch.stack([torch.cos(out), -torch.sin(out), torch.sin(out), torch.cos(out)], dim=-1)
     out = rearrange(out, "b n d (i j) -> b n d i j", i=2, j=2)
-    return out.float()
+    return out.float().to(original_device)
 
 
 def attention_after_rope(q, k, v, pe, mode):
diff --git a/diffusers/src/diffusers/pipelines/realrestorer/pipeline_realrestorer.py b/diffusers/src/diffusers/pipelines/realrestorer/pipeline_realrestorer.py
index 3ed21ab..5734476 100644
--- a/diffusers/src/diffusers/pipelines/realrestorer/pipeline_realrestorer.py
+++ b/diffusers/src/diffusers/pipelines/realrestorer/pipeline_realrestorer.py
@@ -434,10 +434,9 @@ class RealRestorerPipeline(DiffusionPipeline):
             return decoded[0]
         raise ValueError("Unsupported VAE decoder output for RealRestorer.")
 
-    @staticmethod
-    def _autocast_context(device: torch.device):
+    def _autocast_context(self, device: torch.device):
         if device.type == "cuda":
-            return torch.autocast(device_type="cuda", dtype=torch.bfloat16)
+            return torch.autocast(device_type="cuda", dtype=self.dtype)
         return contextlib.nullcontext()
 
     def _denoise_edit(
@@ -583,7 +582,7 @@ class RealRestorerPipeline(DiffusionPipeline):
             ref_images_raw = self.load_image(resized_image).to(device=device, dtype=torch.float32)
             height, width = ref_images_raw.shape[-2:]
             ref_latents_tensor = self._encode_vae_image(ref_images_raw)
-            ref_latents = self._pack_latents(ref_latents_tensor.to(device=device, dtype=torch.bfloat16))
+            ref_latents = self._pack_latents(ref_latents_tensor.to(device=device, dtype=self.dtype))
 
         if height is None or width is None:
             raise ValueError("Both height and width must be resolved before sampling.")
@@ -597,7 +596,7 @@ class RealRestorerPipeline(DiffusionPipeline):
             ),
             generator=generator,
             device=device,
-            dtype=torch.bfloat16,
+            dtype=self.dtype,
         )
         latents = self._pack_latents(noise)
 
